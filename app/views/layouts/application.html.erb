<!DOCTYPE html>
<html ng-app = "summoners-universe">
<head>
  <title>Summoners Universe</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <nav class="navbar">

    <% if user_signed_in? %>
      <ul>
        <li class = "root"><%= link_to "Summoner's Universe", root_path %></li>
        <li class = "non-root"><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li>
        <li class = "non-root"><%= link_to current_user.email, current_user %></li>
      </ul>
    <% else %>
       <ul>
          <li class = "root"><%= link_to "Summoner's Universe", root_path %></li>
          <li class = "non-root"> <%= link_to 'Signup', new_user_registration_path %></li>
          <li class = "non-root"> <%= link_to 'Login', new_user_session_path %></li>
        </ul>
   <% end %>
 </nav>
</header>
  
  <div class = "row"
    <div class="twelve columns"><%= alert %></div>
  </div>
  
    <%= yield %>
</body>
</html>