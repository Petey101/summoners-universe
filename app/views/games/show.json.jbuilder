  json.id @game.id
  json.win @game.win
  json.user_id @game.user
  json.picked_champion @game.picked_champion
  json.completed @game.completed
  json.time_limit @game.time_limit
  json.created_at @game.created_at
  json.updated_at @game.updated_at